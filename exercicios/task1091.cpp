#include <bits/stdc++.h>

using namespace std;

int main(){
    int tiks, clts;
    vector<int> array_tiks;
    for(int i = 0; i < tiks; i++){
        int x;
        cin >> x;
        array_tiks.push_back(x);
    }
    vector<int> array_clts;
    for(int i = 0; i < clts; i++){
        int y;
        cin >> y;
        array_clts.push_back(y);
    }
    sort(array_tiks.begin(), array_tiks.end());
    int x = 0;
    int y = 0;
    for (int i = 0; i < tiks; i++){
        
    }

}